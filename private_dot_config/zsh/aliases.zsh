
# Aliases
alias {v,vi,vim}='nvim'
alias tf='terraform'

alias l='ls -ahl'
alias c='clear'
alias t='tmux attach'
alias cm='chezmoi'

alias bx='bundle exec'

alias hashes='echo "###############################"'

# Docker
alias d="docker"
alias dcb="docker compose build"
alias dcd="docker compose down && docker system prune --volumes -f"
alias dce="docker compose exec"
alias dcr="docker compose run --rm"
alias dcu="docker compose up"
alias dcub="docker compose up --build"

# Git
alias g='git'
alias gcm='git commit -m'
alias gad='git add'
alias gadd='git add .'
alias gdi='clear && git diff --minimal --color-words'
alias gpu='git pull --rebase'
alias gst='clear && git status'
alias gco='clear && git checkout'
